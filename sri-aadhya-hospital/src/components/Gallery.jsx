import { useState } from 'react';import { AnimatePresence,motion } from 'framer-motion';import { X,ChevronLeft,ChevronRight } from 'lucide-react';import { gallery } from '../config/hospital';export default function Gallery(){const [active,setActive]=useState(null);return <section id="gallery" className="py-24 md:py-32 bg-white"><div className="mx-auto max-w-7xl px-5 sm:px-8"><div className="mb-10"><p className="text-sm font-semibold tracking-[0.18em] text-teal-700 uppercase">Inside Sri Aadhya</p><h2 className="mt-4 text-4xl md:text-6xl font-extrabold tracking-tight">A closer look at the hospital.</h2></div><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 auto-rows-[180px] md:auto-rows-[210px]">{gallery.map((g,i)=><button key={g.src+i} onClick={()=>setActive(i)} className={`${i===0?'col-span-2 row-span-2':''} overflow-hidden rounded-2xl bg-slate-100 group text-left`}><img src={g.src} alt={g.alt} loading="lazy" className="w-full h-full object-cover transition duration-700 group-hover:scale-105"/><span className="sr-only">{g.label}</span></button>)}</div></div><AnimatePresence>{active!==null&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[90] bg-slate-950/95 flex items-center justify-center p-4" onClick={()=>setActive(null)}><div className="relative max-w-6xl w-full" onClick={e=>e.stopPropagation()}><img src={gallery[active].src} alt={gallery[active].alt} className="w-full max-h-[80vh] object-contain rounded-2xl"/><button onClick={()=>setActive(null)} className="absolute top-3 right-3 w-11 h-11 rounded-full bg-white/10 text-white grid place-items-center"><X/></button><button onClick={()=>setActive((active-1+gallery.length)%gallery.length)} className="absolute left-2 top-1/2 -translate-y-1/2 w-11 h-11 rounded-full bg-white/10 text-white grid place-items-center"><ChevronLeft/></button><button onClick={()=>setActive((active+1)%gallery.length)} className="absolute right-2 top-1/2 -translate-y-1/2 w-11 h-11 rounded-full bg-white/10 text-white grid place-items-center"><ChevronRight/></button></div></motion.div>}</AnimatePresence></section>}
