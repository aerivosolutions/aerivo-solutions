import { AnimatePresence,motion } from 'framer-motion';import { useAppointment } from './appointment-context';import AppointmentForm from './AppointmentForm';export default function AppointmentModal(){const {open,closeAppointment}=useAppointment();return <AnimatePresence>{open&&<motion.div className="fixed inset-0 z-[100] bg-slate-950/60 backdrop-blur-sm flex items-center justify-center p-4" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={closeAppointment}><motion.div initial={{y:20,opacity:0,scale:.98}} animate={{y:0,opacity:1,scale:1}} exit={{y:20,opacity:0,scale:.98}} className="w-full max-w-2xl rounded-3xl bg-white p-6 md:p-8 shadow-2xl max-h-[90vh] overflow-auto" onClick={e=>e.stopPropagation()}><div className="flex items-start justify-between mb-5"><div><p className="text-sm font-semibold tracking-[0.16em] uppercase text-teal-700">Appointment</p><h3 className="text-2xl md:text-3xl font-bold text-slate-900 mt-1">Request an appointment</h3></div><button onClick={closeAppointment} className="w-10 h-10 rounded-full bg-slate-100 hover:bg-slate-200">×</button></div><AppointmentForm onDone={closeAppointment}/></motion.div></motion.div>}</AnimatePresence>}
